<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
    <title>Add Blogs</title>
    <style>
        /* Premium Add Blog Form Styling */
        body {
            background: linear-gradient(135deg, #1a1d20 0%, #121416 100%);
            color: rgba(255, 255, 255, 0.9);
            min-height: 100vh;
        }

        .container.blog-form-container {
            max-width: 800px;
            padding: 2rem;
            margin-top: 2rem;
            margin-bottom: 3rem;
            background: rgba(33, 37, 41, 0.7);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3), 0 0 20px rgba(13, 202, 240, 0.08);
            position: relative;
            overflow: hidden;
        }

        .container.blog-form-container::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(to right, #0dcaf0, rgba(13, 202, 240, 0.5), rgba(13, 202, 240, 0.1));
        }

        .form-title {
            font-size: 2.2rem;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 1.5rem;
            position: relative;
            padding-bottom: 0.8rem;
        }

        .form-title::after {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            width: 80px;
            height: 3px;
            background: #0dcaf0;
            border-radius: 2px;
        }

        .form-label {
            color: rgba(255, 255, 255, 0.9);
            font-weight: 500;
            font-size: 1rem;
            margin-bottom: 0.7rem;
            letter-spacing: 0.5px;
        }

        .form-control {
            background-color: rgba(30, 34, 38, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.9);
            border-radius: 8px;
            padding: 0.8rem 1.2rem;
            transition: all 0.3s;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
        }

        .form-control:focus {
            background-color: rgba(35, 39, 43, 0.8);
            border-color: rgba(13, 202, 240, 0.5);
            box-shadow: 0 0 0 3px rgba(13, 202, 240, 0.2);
            color: white;
        }

        .form-control::placeholder {
            color: rgba(255, 255, 255, 0.4);
        }

        textarea.form-control {
            min-height: 180px;
            resize: vertical;
        }

        .custom-file-input-wrapper {
            position: relative;
            margin-bottom: 1.5rem;
        }

        .custom-file-label {
            position: absolute;
            top: 0;
            right: 0;
            left: 0;
            z-index: 1;
            padding: 0.8rem 1.2rem;
            font-weight: 400;
            line-height: 1.5;
            color: rgba(255, 255, 255, 0.6);
            background-color: rgba(30, 34, 38, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            transition: all 0.3s;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        input[type="file"] {
            padding: 0.67rem;
        }

        input[type="file"]::file-selector-button {
            margin-right: 1rem;
            padding: 0.5rem 1rem;
            border: none;
            background-color: rgba(13, 202, 240, 0.2);
            color: white;
            font-weight: 500;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.2s;
        }

        input[type="file"]::file-selector-button:hover {
            background-color: rgba(13, 202, 240, 0.3);
        }

        .btn-submit {
            background: linear-gradient(to right, #0dcaf0, rgba(13, 202, 240, 0.8));
            color: #fff;
            font-weight: 600;
            padding: 0.8rem 2rem;
            border-radius: 8px;
            border: none;
            box-shadow: 0 4px 15px rgba(13, 202, 240, 0.3);
            transition: all 0.3s;
            letter-spacing: 0.5px;
            position: relative;
            overflow: hidden;
        }

        .btn-submit::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.2), transparent);
            transform: translateX(-100%);
            transition: transform 0.6s;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(13, 202, 240, 0.4);
        }

        .btn-submit:hover::before {
            transform: translateX(100%);
        }

        .form-section {
            position: relative;
            padding-bottom: 2rem;
        }

        .form-floating-icon {
            position: absolute;
            right: 1rem;
            top: 0.7rem;
            color: rgba(13, 202, 240, 0.6);
            font-size: 1.2rem;
        }

        .hint-text {
            color: rgba(255, 255, 255, 0.5);
            font-size: 0.85rem;
            margin-top: 0.5rem;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .blog-form-container {
            animation: fadeIn 0.5s ease-out forwards;
        }
    </style>
</head>

<body>
    <%- include('./partials/nav') %>

    <div class="container blog-form-container">
        <h2 class="form-title">Create New Blog Post</h2>
        <form action="/blog" method="post" enctype="multipart/form-data">
            <div class="mb-4">
                <label for="coverImage" class="form-label">Cover Image</label>
                <input type="file" class="form-control" name="coverImage" id="coverImage" aria-describedby="coverImage">
                <div class="hint-text">Recommended size: 1200 x 630 pixels (JPG, PNG)</div>
            </div>
            
            <div class="mb-4">
                <label for="title" class="form-label">Title</label>
                <input type="text" class="form-control" name="title" id="title" aria-describedby="title" placeholder="Enter a captivating title..." required>
            </div>
            
            <div class="mb-4">
                <label for="body" class="form-label">Description</label>
                <textarea name="body" class="form-control" id="body" placeholder="Share your thoughts here..." required></textarea>
                <div class="hint-text">Use clear paragraphs and formatting for better readability</div>
            </div>
            
            <div class="mb-3 text-end">
                <button type="submit" class="btn btn-submit">Publish Post</button>
            </div>
        </form>
    </div>
    
    <%- include('./partials/script') %>
</body>

</html>