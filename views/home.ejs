<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Homepage</title>
    <style>
        /* Premium Dark Theme Styling for Homepage - Matching Signin Page */
        body {
            background: linear-gradient(135deg, #1a1d20 0%, #121416 100%);
            color: rgba(255, 255, 255, 0.9);
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
        }
        
        /* Hero Section */
        .hero-section {
            background: rgba(33, 37, 41, 0.7);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            color: white;
            padding: 4rem 0;
            margin-bottom: 3.5rem;
            border-radius: 0 0 25px 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3), 0 0 20px rgba(13, 202, 240, 0.08);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.05);
            animation: fadeIn 0.5s ease-out forwards;
        }
        
        .hero-section::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(to right, #0dcaf0, rgba(13, 202, 240, 0.5), rgba(13, 202, 240, 0.1));
        }
        
        .hero-content {
            max-width: 850px;
            margin: 0 auto;
            padding: 0 1.5rem;
            text-align: center;
            position: relative;
            z-index: 1;
        }
        
        .hero-content h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1.25rem;
            color: #ffffff;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
        }
        
        .hero-content p {
            font-size: 1.1rem;
            opacity: 0.8;
            margin-bottom: 2rem;
            max-width: 650px;
            margin-left: auto;
            margin-right: auto;
            color: rgba(255, 255, 255, 0.7);
        }
        
        /* Blog Grid */
        .blog-grid {
            padding: 0 1rem;
        }
        
        .blog-card {
            background: rgba(33, 37, 41, 0.7);
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 12px;
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            margin-bottom: 2rem;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            height: 100%;
            position: relative;
            animation: fadeIn 0.5s ease-out forwards;
        }
        
        .blog-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(to right, #0dcaf0, rgba(13, 202, 240, 0.3), transparent);
        }
        
        .blog-card:hover {
            transform: translateY(-7px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3), 0 0 15px rgba(13, 202, 240, 0.1);
        }
        
        .card-img-container {
            height: 220px;
            overflow: hidden;
            position: relative;
        }
        
        .blog-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
            transition: transform 0.6s ease;
            opacity: 0.9;
        }
        
        .blog-card:hover img {
            transform: scale(1.08);
            opacity: 1;
        }
        
        .blog-card .card-body {
            padding: 1.75rem;
        }
        
        .blog-card .card-title {
            font-weight: 600;
            font-size: 1.3rem;
            margin-bottom: 0.75rem;
            color: white;
            line-height: 1.4;
        }
        
        .blog-date {
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.85rem;
            margin-bottom: 0.75rem;
            display: block;
            font-weight: 500;
            letter-spacing: 0.5px;
        }
        
        .card-excerpt {
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 1.25rem;
            font-size: 0.95rem;
            line-height: 1.6;
        }
        
        .btn-view {
            background: linear-gradient(to right, #0dcaf0, rgba(13, 202, 240, 0.8));
            border: none;
            padding: 0.6rem 1.5rem;
            border-radius: 8px;
            color: white;
            font-weight: 500;
            transition: all 0.3s ease;
            display: inline-block;
            box-shadow: 0 4px 15px rgba(13, 202, 240, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .btn-view::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.2), transparent);
            transform: translateX(-100%);
            transition: transform 0.6s;
        }
        
        .btn-view:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(13, 202, 240, 0.4);
        }
        
        .btn-view:hover::before {
            transform: translateX(100%);
        }
        
        .section-title {
            position: relative;
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 2.5rem;
            padding-bottom: 1rem;
            text-align: center;
            color: white;
            letter-spacing: 0.5px;
        }
        
        .section-title:after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 60px;
            height: 3px;
            background: linear-gradient(to right, #0dcaf0, rgba(13, 202, 240, 0.5));
            border-radius: 2px;
        }
        
        .blog-empty {
            text-align: center;
            padding: 4rem 2rem;
            background: rgba(33, 37, 41, 0.7);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            animation: fadeIn 0.5s ease-out forwards;
        }
        
        .blog-empty h3 {
            font-weight: 600;
            color: white;
            margin-bottom: 1rem;
        }
        
        .blog-empty p {
            color: rgba(255, 255, 255, 0.7);
            font-size: 1.1rem;
        }
        
        /* Category badges */
        .category-badge {
            background-color: rgba(13, 202, 240, 0.15);
            color: rgba(13, 202, 240, 0.9);
            font-size: 0.75rem;
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            margin-right: 0.5rem;
            display: inline-block;
            font-weight: 500;
            margin-bottom: 0.75rem;
            border: 1px solid rgba(13, 202, 240, 0.2);
        }
        
        /* Newsletter section */
        .newsletter-section {
            background: rgba(33, 37, 41, 0.7);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            padding: 3rem 2rem;
            margin: 4rem 0;
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.05);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3), 0 0 20px rgba(13, 202, 240, 0.08);
            position: relative;
            overflow: hidden;
            animation: fadeIn 0.5s ease-out forwards;
        }
        
        .newsletter-section::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(to right, #0dcaf0, rgba(13, 202, 240, 0.5), rgba(13, 202, 240, 0.1));
        }
        
        .newsletter-container {
            max-width: 650px;
            margin: 0 auto;
            text-align: center;
            padding: 0 1.5rem;
            position: relative;
            z-index: 1;
        }
        
        .newsletter-title {
            font-size: 1.75rem;
            font-weight: 700;
            color: white;
            margin-bottom: 1rem;
        }
        
        .newsletter-container p {
            color: rgba(255, 255, 255, 0.7);
            font-size: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .newsletter-form {
            display: flex;
            margin-top: 1.5rem;
            gap: 0.5rem;
        }
        
        .newsletter-input {
            flex: 1;
            background-color: rgba(30, 34, 38, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.9);
            border-radius: 8px;
            padding: 0.8rem 1.2rem;
            font-size: 1rem;
            outline: none;
            transition: all 0.3s;
        }
        
        .newsletter-input:focus {
            background-color: rgba(35, 39, 43, 0.8);
            border-color: rgba(13, 202, 240, 0.5);
            box-shadow: 0 0 0 3px rgba(13, 202, 240, 0.2);
            color: white;
        }
        
        .newsletter-input::placeholder {
            color: rgba(255, 255, 255, 0.4);
        }
        
        .btn-subscribe {
            background: linear-gradient(to right, #0dcaf0, rgba(13, 202, 240, 0.8));
            border: none;
            border-radius: 8px;
            padding: 0.8rem 1.5rem;
            font-weight: 600;
            color: white;
            cursor: pointer;
            transition: all 0.3s;
            white-space: nowrap;
            box-shadow: 0 4px 15px rgba(13, 202, 240, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .btn-subscribe::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.2), transparent);
            transform: translateX(-100%);
            transition: transform 0.6s;
        }
        
        .btn-subscribe:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(13, 202, 240, 0.4);
        }
        
        .btn-subscribe:hover::before {
            transform: translateX(100%);
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        @media (max-width: 992px) {
            .row-cols-lg-3 {
                --bs-row-cols: 2 !important;
            }
            
            .newsletter-form {
                flex-direction: column;
            }
            
            .btn-subscribe {
                width: 100%;
            }
        }
        
        @media (max-width: 768px) {
            .row-cols-md-2 {
                --bs-row-cols: 1 !important;
            }
            
            .hero-content h1 {
                font-size: 2rem;
            }
            
            .hero-content p {
                font-size: 1rem;
            }
            
            .section-title {
                font-size: 1.75rem;
            }
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="hero-content">
            <h1>Welcome to Blogging Page</h1>
            <p>Let's code and Help each other to Solve Daily DSA Problems</p>
        </div>
    </section>

    <div class="container">
        <h2 class="section-title">Latest Articles</h2>
        
        <div class="blog-grid">
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                <% if (blogs && blogs.length > 0) { %>
                    <% blogs.forEach(blog => { %>
                        <div class="col">
                            <div class="blog-card card">
                                <div class="card-img-container">
                                    <img src="<%= blog.coverImageURL || '/default-placeholder.jpg' %>" class="card-img-top" alt="<%= blog.title %>">
                                </div>
                                <div class="card-body">
                                    <% if (blog.category) { %>
                                        <span class="category-badge"><%= blog.category %></span>
                                    <% } %>
                                    <% if (blog.createdAt) { %>
                                        <small class="blog-date"><%= new Date(blog.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></small>
                                    <% } %>
                                    <h5 class="card-title"><%= blog.title %></h5>
                                    <% if (blog.excerpt) { %>
                                        <p class="card-excerpt"><%= blog.excerpt.substring(0, 120) %>...</p>
                                    <% } %>
                                    <a href="/blog/<%= blog._id %>" class="btn btn-view">Read More</a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="col-12">
                        <div class="blog-empty">
                            <h3>No blog posts yet</h3>
                            <p>Check back soon for new content!</p>
                        </div>
                    </div>
                <% } %>
            </div>
        </div>
        
        <!-- Newsletter Section -->
        <section class="newsletter-section">
            <div class="newsletter-container">
                <h3 class="newsletter-title">Subscribe to our Newsletter</h3>
                <p>Stay up to date with our latest articles and updates delivered straight to your inbox.</p>
                <form class="newsletter-form">
                    <input type="email" class="newsletter-input" placeholder="Enter your email address" required>
                    <button type="submit" class="btn-subscribe">Subscribe</button>
                </form>
            </div>
        </section>
    </div>

    <%- include('./partials/script') %>
</body>
</html>